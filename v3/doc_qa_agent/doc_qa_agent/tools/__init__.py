"""Tool implementations used by the agent.

Modules:
* ``repo`` — the imposed GitLab ``clone`` helper.
* ``execution`` — venv/install/run-file/run-snippet/run-notebook tools.
* ``retrieval`` — FAISS RAG indexing and search.
* ``storage`` — the imposed Qdrant ("sparrow") vector store.
"""
